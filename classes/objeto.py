# -*- coding: utf-8 -*-
import pygame, sys, os, time, random
from pygame.locals import *
'''
class Objeto:
	imagem = None

	def __init__ (self, imagem):
		pygame.init()
		caminho = os.path.join("imagens", str(imagem))
		self = pygame.image.load(caminho).convert_alpha()
'''